a6300 wifi

(getVersions)
getWebApiVersionsCamera
getWebApiVersionsAvContent
getWebApiVersionsSystem
getWebApiVersionsGuide
getWebApiVersionsAccessControl

(getMethodTypes)
getMethodTypesCamera
getMethodTypesAvContent
getMethodTypesSystem
getMethodTypesGuide
getMethodTypesAccessControl

getAvailableFunctions (getAvailableApiList)

getApplicationInfo (getApplicationInfo)

getAvailableSettings (getEvent (v1.0), getEvent (v1.1), getEvent (v1.2), getEvent (v1.3))

getCameraFunction (getCameraFunction, getSupportedCameraFunction, getAvailableCameraFunction)
setCameraFunction (setCameraFunction)

actCapturePhoto (actTakePicture)
awaitCapturePhoto (awaitTakePicture)

startRecMode (startRecMode)
stopRecMode (stopRecMode)

getFNumber (getFNumber, getSupportedFNumber, getAvailableFNumber)
modifyFNumber
setFNumber (setFNumber)

getIso (getIsoSpeedRate, getSupportedIsoSpeedRate, getAvailableIsoSpeedRate)
modifyIso
setIso (setIsoSpeedRate)

getShutterSpeed (getShutterSpeed, getSupportedShutterSpeed, getAvailableShutterSpeed)
modifyShutterSpeed
setShutterSpeed (setShutterSpeed)

getExposureCompensation (getExposureCompensation, getSupportedExposureCompensation, getAvailableExposureCompensation)
modifyExposureCompensation
setExposureCompensation (setExposureCompensation)

getFlashMode (getFlashMode, getSupportedFlashMode, getAvailableFlashMode)
setFlashMode (setFlashMode)

getFocusMode (getFocusMode, getSupportedFocusMode, getAvailableFocusMode)
setFocusMode (setFocusMode)

getZoomSetting (getZoomSetting, getSupportedZoomSetting, getAvailableZoomSetting)
setZoomSetting (setZoomSetting)

getStorageInformation (getStorageInformation)
actZoom (actZoom)

actHalfPressShutter (actHalfPressShutter)
cancelHalfPressShutter (cancelHalfPressShutter)

getSelfTimer (getSelfTimer, getSupportedSelfTimer, getAvailableSelfTimer)
setSelfTimer (setSelfTimer)

getLiveViewSize (getLiveviewSize, getSupportedLiveviewSize, getAvailableLiveviewSize)
startLiveViewWithSize (startLiveviewWithSize)

getPostViewImageSize (getPostviewImageSize, getSupportedPostviewImageSize, getAvailablePostviewImageSize)
setPostViewImageSize (setPostviewImageSize)

getProgramShift (setProgramShift)
setProgramShift (getSupportedProgramShift)

getWhiteBalanceMode (getWhiteBalance, getSupportedWhiteBalance, getAvailableWhiteBalance)
setWhiteBalanceMode (setWhiteBalance)

getWhiteBalanceColorTemp
modifyWhiteBalanceColorTemp
setWhiteBalanceColorTemp

getImageFileFormat (setStillQuality)
setImageFileFormat (getStillQuality, getSupportedStillQuality, getAvailableStillQuality)

getShootMode (setShootMode)
getShootMode (getShootMode, getSupportedShootMode, getAvailableShootMode)

setContShootingSpeed (setContShootingSpeed)
getContShootingSpeed (getContShootingSpeed, getSupportedContShootingSpeed, getAvailableContShootingSpeed)

setContShootingMode (setContShootingMode)
getContShootingMode (getContShootingMode, getSupportedContShootingMode, getAvailableContShootingMode)

startContShooting (startContShooting)
stopContShooting (stopContShooting)

startMovieRec (startMovieRec)
stopMovieRec (stopMovieRec)

startAudioRec (startAudioRec)
stopAudioRec (stopAudioRec)

startIntervalStillRec (startIntervalStillRec)
stopIntervalStillRec (stopIntervalStillRec)

startLoopRecording (startLoopRec)
stopLoopRecording (stopLoopRec)

startLiveView (startLiveview)
stopLiveView (stopLiveview)

setLiveViewFrameInfo (setLiveviewFrameInfo)
getLiveViewFrameInfo (getLiveviewFrameInfo)

getSilentShooting
setSilentShooting







getMeteringMode
setMeteringMode

getAel (usb)
getAspectRatio
getAutoFocusState
getBatteryPercentage
getDriveMode
getDroHdr
getFel
getFlashValue
getFocusArea
getFocusAreaSpot
getFocusMagnifier
getFocusMagnifierDirection
getFocusMagnifierPhase
getFocusModeToggle
getImageSize
getPhotoAvailable
getPictureEffect
getWhiteBalanceAb
getWhiteBalanceGm
pressShutter
releaseShutter
requestPhotoAvailable
setAel
setAspectRatio
setDriveMode
setDroHdr
setFel
setFlashValue
setFocusArea
setFocusAreaSpot
setFocusDistance
setFocusMagnifier
setFocusMagnifierDirection
setFocusMagnifierPhase
setFocusModeToggle
setImageSize
setPictureEffect
setSettingsRaw
setWhiteBalanceAb
setWhiteBalanceGm

startRecordingVideo
stopRecordingVideo

startRecordingAudio
stopRecordingAudio
setRecordingAudio

getRecordingAudio
getRecordingVideoState

streamLiveView